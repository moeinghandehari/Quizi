object Identifiers {
    object SigningConfigs {
        const val DEBUG = "debug"
        const val RELEASE = "release"
    }

    object BuildTypes {
        const val DEBUG = "debug"
        const val RELEASE = "release"
    }

    object Params {
        const val APP_CENTER_APP_SECRET = "appCenterAppSecret"
        const val VERSION_CODE = "versionCode"
        const val SIGNING_KEY_ALIAS = "signingKeyAlias"
        const val SIGNING_KEY_PASSWORD = "signingKeyPassword"
        const val SIGNING_KEYSTORE_PASSWORD = "signingKeystorePassword"
        const val SIGNING_KEYSTORE_PATH = "signingKeystorePath"
        const val IS_MINIFY_ENABLED = "isMinifyEnabled"
    }
}